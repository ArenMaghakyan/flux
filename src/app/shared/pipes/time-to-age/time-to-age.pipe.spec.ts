import { TimeToAgePipe } from './time-to-age.pipe';

describe('TimeToAgePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeToAgePipe();
    expect(pipe).toBeTruthy();
  });
});
